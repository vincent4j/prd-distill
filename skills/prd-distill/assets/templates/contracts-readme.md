# PRD 合同索引

合同是 PRD 中必须被测试、日志或运行证据证明的硬约束。

## 规则

- 合同必须放在 `docs/prd/contracts/` 下。
- 草稿放在 `docs/prd/contracts/inbox/`。
- 生效合同必须关联 PRD 章节、测试和运行证据。
- 合同按模块归档，例如 `example-module.md`。

## 模块合同

| 模块 | 合同文件 | 关联 PRD |
|---|---|---|
